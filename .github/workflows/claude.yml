name: Claude Code

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]

jobs:
  claude:
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review' && contains(github.event.review.body, '@claude')) ||
      (github.event_name == 'issues' && (contains(github.event.issue.body, '@claude') || contains(github.event.issue.title, '@claude')))
    runs-on: ubuntu-latest
    permissions:
      contents: write          # allow committing/pushing
      pull-requests: write     # allow commenting/PR updates
      issues: write            # allow commenting on issues
      id-token: write
      actions: read            # for reading CI results
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0                   # full history if pushing
          token: ${{ secrets.MY_MY_MY_GITHUB_TOKEN }}  # use the job's write-scoped token

      - name: Run Claude Code
        id: claude
        uses: anthropics/claude-code-action@beta
        with:
          # Pass the workflow token explicitly as the action requests
          MY_MY_MY_GITHUB_TOKEN: ${{ secrets.MY_MY_MY_GITHUB_TOKEN }}

          # Anthropic OAuth token (your existing secret)
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}

          # Optional: allow reading CI results
          additional_permissions: |
            actions: read

          # Other optional inputs...
          # model: "claude-opus-4-1-20250805"
          # trigger_phrase: "/claude"
          # assignee_trigger: "claude-bot"
          # allowed_tools: "Bash(npm install),Bash(npm run build),Bash(npm run test:*),Bash(npm run lint:*)"
          # custom_instructions: |
          #   Follow our coding standards
          #   Ensure all new code has tests
          #   Use TypeScript for new files
          # claude_env: |
          #   NODE_ENV: test